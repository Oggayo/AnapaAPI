CREATE DATABASE anapa;
CREATE TABLE users(
	id int AUTO_INCREMENT PRIMARY KEY,
    balance int NOT NULL,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL,
    password VARCHAR(50) NOT NULL
);